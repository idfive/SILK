//- ========================================
//- SVG Symbol Inclusion
//- ========================================

mixin symbol(name)
  svg(class="symbol symbol-#{name}")
    use(xlink:href="##{name}")
